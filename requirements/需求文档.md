# artDatabase 需求文档

> 汇总文档：整合项目现状事实、现阶段需求与更新需求记录。
> 最后更新：2026-08-17
> 本文档是需求的唯一总入口；`requirements/document/` 下按「日期 + 版本」命名的文件保留为历史存档。

## 一、项目现状事实

### 1.1 已完成功能

核心功能已实现并可本地运行：

- **项目管理**：创建、重命名、删除项目。
- **项目维度**：创建/删除维度，维度值通过 Slider 连续编辑（`0.00～10.00`，保留两位小数）。
- **图片上传**：文件选择、拖拽、剪贴板粘贴；JPEG/PNG/WebP 单图上传，单文件上限 50MB；上传前 SHA-256 完全重复检测，可引用已有素材或保留为新素材。
- **素材浏览与筛选**：统一尺寸网格 + 列表切换，按名称/文件名/标签/引用项目搜索；全部素材页完整展示全局标签，支持单标签快速筛选并与关键词组合。
- **维度预览**：一维轴 / 二维平面 / 三维空间自动对比，单次预览最多选择 3 个项目维度。
- **自由画板**：一个项目多张画板，创建/切换/重命名/删除；元素拖动、缩放、旋转、置顶/置底/移除；布局持久化，多人短轮询同步（不同元素互不覆盖，同一元素最后写入）。
- **素材详情与 Metadata 编辑**：原图等比例完整预览；名称、全局标签、描述、备注、来源链接可编辑；项目详情可编辑全部项目维度值；全局修改同步到所有引用项目。
- **安全删除交互**：删除已有全局标签或项目维度时延迟 5 秒执行，倒计时期间可撤销。

### 1.2 当前技术架构

| 层 | 现状 |
| --- | --- |
| 应用运行时 | 原生 Node.js（`node:http`，单进程） |
| 结构化业务数据 | SQLite（Node 内置 `node:sqlite`） |
| 图片二进制与缩略图 | 本地磁盘 + `sharp` 服务端生成缩略图 |
| Metadata | SQLite `assets` 表（唯一来源） |
| 前端 | React 19 + TypeScript + Vite + Tailwind v4 |

数据表：`projects`、`project_dimensions`、`assets`、`project_assets`、`asset_dimension_values`、`canvases`、`canvas_items`。维度值以整数 `0～1000` 存储，对应 `0.00～10.00`，避免浮点精度问题。

### 1.3 需求状态概览

- 需求基线 v0.1 已完成，并被 v0.4 及 v0.6～v0.14 部分修订，v0.4 至今保持「当前有效」。
- v0.15「后端图片存储去 ResourceSpace 重构」已由 v0.16 实施：改用原生 Node.js 单服务，原图与缩略图落本地磁盘。
- v0.17「素材详情与全局素材交互修订」已完成：详情完整原图预览、Metadata 编辑、全局标签筛选、遮罩关闭和标签/维度 5 秒撤销删除均已实现。

## 二、现阶段需求

### 2.1 项目目标与 MVP 范围

面向团队的图片素材库：素材文件与项目相互独立，项目通过引用组织素材；全局标签用于检索筛选，项目维度用于项目内排序与对比；自由画板是 MVP 核心功能。

- 首版只支持图片；视频、音频留作后续扩展。
- 本地部署，通过局域网提供服务。
- 不做用户注册、登录与权限；不支持批量上传；暂不要求备份。

### 2.2 核心概念

- **全局素材库**：团队共用，每个素材一条主记录、一份唯一 Metadata，素材不属于任何单一项目。
- **项目**：一级分类，通过引用（不复制文件）组织素材；同一素材可加入多个项目；Metadata 修改后所有引用项目同步。
- **Metadata**：与素材一一对应，字段见下表。
- **标签**：两个作用域——素材自身标签（全局，自由输入）与项目维度（项目级，左右端点 + 连续数值）。

MVP Metadata 字段：

| 字段 | 来源 | 可空 | 说明 |
| --- | --- | --- | --- |
| 素材名称 | 用户编辑 | 否 | 上传时默认使用去扩展名的原始文件名，之后可编辑 |
| 素材标签 | 用户编辑 | 是 | 全局自由输入 |
| 描述 | 用户编辑 | 是 | 对素材内容的描述 |
| 备注 | 用户编辑 | 是 | 团队内部补充信息 |
| 来源链接 | 用户编辑 | 是 | 素材原始来源 URL |
| 上传时间 / 原始文件名 / 文件大小 / 图片尺寸 / 文件格式 | 系统生成 | 否 | 自动记录 |
| 时长 | 系统生成 | 是 | 图片素材为空，为视频和音频预留 |

### 2.3 核心功能

- **素材浏览**：查看全局素材库与项目素材；全部素材页展示所有全局标签，支持单标签快速筛选并与关键词搜索组合；项目内按项目维度组织和预览。
- **项目页面**：默认展示项目全部素材，统一尺寸网格、居中裁剪缩略图、Hover 放大预览。
- **上传**：见 1.1；正式提交前弹出 Metadata 编辑窗口（全局标签 + 当前项目全部维度 Slider）；新素材自动加入当前项目并为全部维度创建赋值（默认 `5.00`）；取消则不创建记录、不提交上传。
- **移除与删除**：区分「从项目移除」（仅删引用）与「全局删除」（进回收站：查看/恢复/彻底删除）；删除已有全局标签或项目维度时先进入 5 秒待删除状态，期间允许撤销。
- **自动对比**：按所选维度数量使用一维轴 / 二维平面 / 三维空间排列，调整 Slider 后位置同步更新。
- **自由画板**：多画板、持久化布局、局域网多人共同编辑与同步。
- **项目管理**：创建/重命名/删除/复制；删除项目不删全局素材；复制项目时复制维度配置（是否复制素材引用与画板待确认）。

### 2.4 维度规则（v0.4 澄清后）

- 一个项目可创建**任意数量**维度，无「3 个」的项目级上限。
- 「最多 3 个」指单次维度预览最多选择 3 个项目维度作为坐标轴（1 → 一维轴，2 → 二维平面，3 → 三维空间）。
- 上传与编辑素材时显示并保存当前项目的全部维度值，不受预览选择上限影响。
- 维度值范围 `0.00～10.00`，保留两位小数，Slider 连续调整。
- 素材加入项目或项目新增维度时，对应维度值默认中点 `5.00`。
- 数据库以 `(projectId, assetId, dimensionId)` 唯一约束同一素材在同一项目同一维度下只有一个值。

### 2.5 三维预览交互规则（v0.6～v0.14 合并）

- 三轴从中心向正负两端放射、在中心相交；不显示承载平面或边框；轴两端显示维度左右端名称。
- 所有图片始终面向摄像机，不随旋转倾斜。
- 中键拖动改变视角；左键拖动图片改变前两个维度值；`Shift` + 左键拖动改变第三（深度）维度值；松开后写入服务端。
- 水平旋转完整 `360°` 循环，方向与鼠标移动一致；俯仰角范围 `0°～180°`，到两端停止、不循环。
- XY 平面为透明正方形网格，按 `0～10` 每 `1` 单位一格；网格平面仅作视觉参照、不参与鼠标命中，图片单独接收指针事件。
- 拖动图片按当前视角反投影，并保留按下点与图片中心的偏移，起步不跳变。
- 维度预览最小高度 `640px`，滚轮缩放 `35%～280%`；左侧菜单可隐藏；支持页面内「专注模式」（不调用浏览器 Fullscreen）。

### 2.6 后端架构（v0.16 重构后）

- 应用为**单个原生 Node.js 进程**：`node:http` 同时提供前端页面与 `/api/*` 接口，前端 + 接口 + SQLite + 图片存储合一，不依赖 Cloudflare Worker / D1 / ResourceSpace。
- 数据库用 Node 内置 `node:sqlite`（SQLite 文件，默认 `app/data/app.db`），表结构与 D1 时期一致，移除废弃的 `external_id` 列。
- 图片与缩略图落本地磁盘（默认 `app/data/media/`）：原图 `blobs/<id>`、缩略图 `thumbs/<id>`；缩略图由服务端 `sharp` 生成（最长边 ≤ 900px 的 WebP），客户端不再生成。
- `/api/*` URL 契约不变，前端组件原样复用；上传前 SHA-256 重复检测在 SQLite 中进行。
- 软删/恢复只改 `deleted_at`，仅「彻底删除」删除磁盘文件。
- 运行时依赖仅 `sharp` + React 等前端库，无框架、无 ORM、无数据库驱动；环境要求 Node.js ≥ 23.4。

### 2.7 素材详情与全局素材交互（v0.17）

- 详情左侧优先展示原图，图片同时受容器宽、高约束并保持原始比例，必须完整可见，禁止裁切。
- 打开详情后锁定后方页面滚动；右侧详情面板独立滚动；点击图片外黑色遮罩或按 `Esc` 关闭，点击图片本身不关闭。
- 通过关闭按钮、黑色遮罩或 `Esc` 关闭详情时，自动提交已修改的素材信息，与“保存素材信息”行为一致；表单校验或保存失败时不关闭。
- 全局详情与项目详情都可编辑素材名称、全局标签、描述、来源链接和备注；项目详情额外编辑当前项目全部维度值。
- 素材网格和列表卡片不展示原始文件名；原始文件名仅在详情中作为只读信息展示。
- “全部素材 + 总数”与项目标题使用同一顶部栏样式；取消独立大标题区、未归档计数、永久删除说明和全局预览说明。
- 全部素材页完整展示全局标签；点击单个标签快速筛选，再次点击取消；标签筛选与关键词搜索使用 AND 组合。
- 删除已有全局标签或项目维度时显示 5 秒倒计时 Toast；倒计时结束后才写数据库，期间可撤销；关闭详情或切换页面不终止计时。

### 2.8 当前明确不做

- 不做图片裁剪、调色、加字等在线编辑；不做 Metadata 历史版本。
- 不复制素材文件到各项目；不支持视频与音频；不支持批量上传。
- 不提供用户账号、登录与权限分级；不提供自动备份。

### 2.9 待确认问题（合并自 v0.1 与 v0.15）

1. 图片在对比视图中重叠时，采用堆叠、自动避让还是聚合展示？
2. 标签筛选与维度视图能否同时使用？
3. Hover 放大预览采用原位放大、浮层还是放大镜？
4. 完全重复检测用文件哈希是否满足需求？提醒时是否允许直接引用已有素材？
5. 多人同时移动同一画布元素采用最后写入还是锁定？是否需要画布评论/文字/连线/导出？
6. 是否允许团队自定义 Metadata 字段？
7. 复制项目时是否复制素材引用与自由画布？
8. 未来 NAS 部署形态、支持的图片格式与单文件大小上限？
9. 回收站 UI 尚未提供（软删已置 `deleted_at`）；前端 `UploadModal` 客户端缩略图生成逻辑已冗余，待清理。

### 2.10 部署与运行

本地运行（开发模式）：

```bash
cd app
npm install
npm run dev
```

浏览器访问 `http://localhost:3000`。一个进程同时提供前端页面与 `/api/*` 接口，前端改动即时热更新。

生产运行：

```bash
npm run build   # 打包前端到 dist/
npm start       # 单进程服务 dist/ + 接口
```

验证：

```bash
npm test          # 后端接口全链路冒烟测试
npm run typecheck # 前端类型检查
```

配置项（环境变量）：

| 变量 | 含义 | 默认 |
| --- | --- | --- |
| `PORT` | 监听端口 | `3000` |
| `DB_PATH` | SQLite 数据库文件 | `data/app.db` |
| `STORE_ROOT` | 图片存储目录 | `data/media` |

环境要求：Node.js ≥ 23.4（使用内置 `node:sqlite`）。

云端部署：

- 应用为单进程、单服务，可打包成单容器（或直接跑 Node）部署到任意 VPS / Fly.io / Railway / Render。
- **必须挂载持久化卷**：数据库与图片文件落本地磁盘，重启后依靠持久化卷保留；未挂载卷会导致重启后丢数据与图片。
- 前端、接口、存储都在同一进程，不再有「Worker 与存储服务连通 / Token / 隧道」问题。
- 资源与成本：单文件上限 50MB、缩略图最长边 900px，直接影响存储与带宽用量；MVP 暂不要求自动备份，云端建议为持久化卷另行配置备份。

## 三、更新需求记录（时间倒序）

| 日期 | 版本 | 内容摘要 | 完成情况 |
| --- | --- | --- | --- |
| 2026-08-17 | v0.17 | 素材详情与全局素材交互：完整原图预览、详情 Metadata 编辑、全局标签筛选、遮罩关闭、标签/维度 5 秒撤销删除 | 已完成 |
| 2026-08-16 | v0.16 | 后端重构为原生 Node 单服务：移除 Cloudflare/D1/ResourceSpace | 已完成 |
| 2026-08-16 | v0.15 | 后端图片存储去 ResourceSpace 重构：改用原生 Node.js 图片存储服务 | 已完成（实现见 v0.16） |
| 2026-08-14 | v0.14 | 平面指针穿透：XY 网格平面不参与鼠标命中，图片单独接收事件 | 已完成 |
| 2026-08-14 | v0.13 | 俯仰角区间调整为 `0°～180°`，取消循环与负角度重复 | 已完成 |
| 2026-08-14 | v0.12 | 反转中键横向拖动与水平视角旋转的映射 | 已完成 |
| 2026-08-14 | v0.11 | XY 网格显示区域固定为正方形，不随视口变形 | 已完成 |
| 2026-08-14 | v0.10 | XY 透明网格、拖动反投影、俯仰范围扩展为 `-180°～180°` | 已完成 |
| 2026-08-14 | v0.9 | 视图专注模式：替代浏览器全屏，覆盖页面可用区域 | 已完成 |
| 2026-08-14 | v0.8 | 视图布局与导航：最小高度、滚轮缩放、菜单隐藏 | 已完成（被 v0.9 部分修订） |
| 2026-08-14 | v0.7 | 三维视角范围：水平 `360°` 循环、俯仰 `-88°～88°` | 已完成 |
| 2026-08-14 | v0.6 | 三维预览交互：中心放射轴、图片面向摄像机、Shift 改深度 | 已完成 |
| 2026-08-14 | v0.5 | 上传、预览与画板实现记录 | 已完成 |
| 2026-08-14 | v0.4 | 维度规则澄清：项目维度不限，单次预览最多 3 个 | 当前有效 |
| 2026-08-14 | v0.3 | 第一批功能实现：项目/维度/网格/搜索 | 已完成（被 v0.4 部分修订） |
| 2026-08-14 | v0.2 | 技术验证计划 | 验证一通过，其余暂停 |
| 2026-08-14 | v0.1 | 需求说明（需求基线） | 已完成（被 v0.4 及后续修订） |
