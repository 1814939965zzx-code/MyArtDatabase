# 需求变更记录

这里仅记录需求变化摘要。当前有效规则见 [CURRENT.md](./CURRENT.md)；完整原始文档位于 [`archive/`](./archive/)，默认不应作为 Agent 上下文读取。

| 日期 | 版本 | 变化 | 状态 |
| --- | --- | --- | --- |
| 2026-08-27 | v0.70 | 自由画板文本组件完整文字格式：水平对齐（左/中/右）、垂直对齐（顶/中/底）、加粗/斜体/下划线/删除线、**自动宽度**与**自动高度**模式（新建文本默认开启，框贴合内容；关闭时框宽/框高写为当前内容尺寸），整段文字统一格式存于 payload；**悬浮格式面板新增字号输入**（8～120，编辑中改字号会先提交未保存文字）；选中**或正在编辑**文本元素时弹出悬浮格式面板（面板操作先提交未保存的编辑内容）；文本/图形与图片共用框选多选与整体移动；文本/图形**与图片**选中均显示八个缩放把手（四角自由缩放、四边单轴缩放，锚定对侧），自动宽/高的文本从当前内容尺寸开始缩放、且**按被拖动的轴退出自动尺寸**（拖宽度边保持自动高度、拖高度边保持自动宽度、拖角才两个都退出）；**文本编辑改为回车换行**（`Ctrl/Cmd+Enter`、`Esc` 或点击空白结束编辑），编辑已有文本时预填原文（含多行）；**修复两处文本重复 bug**：① 编辑→显示切换时 React 在同一 contentEditable DOM 上插入渲染节点导致文字重复成两行（编辑器/显示态改用不同 key 强制重建 DOM）；② 中文输入法按 Enter 确认候选词被误提交（Enter 处理忽略 IME 组合态）；多行保存改用 `innerText` 读取（`<br>`/块级换行不丢失）；自动宽度文本显示改为 `pre-wrap` + 限宽（短文本贴合内容、长文本在限宽内自动换行、手动换行保留）；**文本提交改为按元素 id 直接保存**（点击空白退出编辑时框选会清空选中，原实现因依赖选中状态而丢文本） | 进行中 |
| 2026-08-27 | v0.69 | 整体页面结构重构为 Figma 式主页：登录后进入**主页**（左侧导航：项目/全部素材/标签管理/回收站 + 新建项目；右侧项目卡片网格，卡片含封面拼图（最多 4 张素材缩略图）、名称、说明与素材/维度数，hover 提供重命名/删除，顶栏搜索按项目过滤）；点击项目卡片进入**项目工作区**（左侧导航收起、左上“返回主页”按钮、保留顶栏与 维度预览/自由画板/素材库 页签）；素材详情抽屉与上传只存在于项目内；`GET /api/projects` 新增 `thumbnails` 封面字段 | 进行中 |
| 2026-08-26 | v0.68 | 上传窗口新增“AI 打标”入口（与素材详情页一致）：“全局标签”输入框旁提供“AI 打标”按钮，对选中的待上传图片调用同一打标链路返回建议标签（复用/新建数量以 toast 提示），建议标签合并进标签输入框供编辑确认后随上传提交；新增 `POST /api/uploads/ai-tags`（不落库、不建立素材关联、不改动标签字典） | 进行中 |
| 2026-08-26 | v0.67 | 文本字号调节：标记工具条新增“字号”数字输入（8～120）；选中文本时调整并实时保存当前文本字号，未选中时作为新建文本的默认字号；选中文本元素时输入框自动跟随其字号；`Esc` 退出文本编辑并回退到移动工具（指针） | 进行中 |
| 2026-08-26 | v0.66 | 取消“标记图层”概念：文本/图形与图片**混排在同一 z 序**（共用置顶/置底，新元素置顶），不再有独立图层开关；保留**一键隐藏全部标记**（工具条开关，仅隐藏显示不影响数据与编辑，选中绘图工具自动恢复显示）；**Frame 始终在最底层**，元素 z 序不会低于 Frame（置底下限 0）；框选在标记隐藏时只框选图片 | 进行中 |
| 2026-08-26 | v0.65 | 自由画板工具快捷键：`V` 选择（移动）、`T` 文本、`R` 矩形、`O` 椭圆；按 `T/R/O` 自动打开标记图层，`V` 切回指针；输入框/文本编辑中不触发；工具栏按钮 title 与底部提示文案同步标注快捷键 | 进行中 |
| 2026-08-26 | v0.64 | 标记图层文本元素的缩放改为**只改变文本框展示范围（宽/高），字号保持不变**：文本按新框宽重新换行，不再随缩放等比放大/缩小字体 | 进行中 |
| 2026-08-26 | v0.63 | 形状与 Frame 的归属检测改为**按真实几何相交**：椭圆用“矩形仿射到单位圆空间 + 圆-矩形最近点”精确判定，直线/箭头用手绘笔迹用线段-矩形相交（手绘任一段碰到即算），矩形/文本/图片仍按包围盒；创建时、移动松手时、加载归一化时统一使用该判定（标记移动松手同样补记归属）；几何数学通过独立用例验证 | 进行中 |
| 2026-08-26 | v0.62 | 修复拖 Frame 时框内图片漂移的根因：拖动位移被反复叠加（`当前坐标 + 绝对位移` 在每次 pointermove 上累积，图片越拖越超前于 Frame）；改为拖动开始时记录跟随元素的起点坐标，整个拖动过程始终按 `起点 + 绝对位移` 计算，Frame 与其内元素严格同步；同步排查确认图片拖动、标记移动、框选多移均已采用起点位移，无同类叠加问题 | 进行中 |
| 2026-08-26 | v0.61 | 自由画板坐标与归属模型理顺：放开画布元素 `x/y` 的负数坐标下限（无限画布平移/缩放到原点附近时不再被钳回 `x=0`，修复“绘图吸附到画板内部/图片漂移”）；元素（图片+标记）与 Frame 改为**有交集即归属**（按重叠面积取最大者，替代原先的“中心点”判定），拖 Frame 时按拖动开始时的交集集合同步位移（图片/标记均跟随，Illustrator 式），松手后补记归属；删除 Frame 仍移除其中图片布局（素材保留）；**去掉 Frame 自动撑大逻辑**——Frame 固定尺寸、图片可溢出，拖图片到边界不再改变 Frame 尺寸；图片/标记/Frame 移动坐标全程浮点不取整；多选删除不再弹确认直接删除；托盘标签改为“展开/收起”按钮（默认收起，点击展开全部标签，搜索时自动展开并按搜索词过滤标签与图片） | 进行中 |
| 2026-08-26 | v0.60 | 自由画板交互再细化：框选（marquee）在所有模式可用（含标记图层打开时），可同时框选图片与标记元素；图片“中心落在哪个 Frame 内就归属哪个 Frame”，移动松手时按中心自动重归属、加载时归一化，图片随 Frame 位移/删除生效（修复拖入 Frame 的图片不跟随）；素材托盘标签由下拉改为全部展开的标签 chip（点击筛选、可拖动批量插入），搜索词同时过滤标签与图片；打开标记图层不再锁定其他操作——指针工具下图片/Frame/标记/框选全部可操作，仅选中绘图工具（笔/矩形/椭圆/箭头/直线/文本）时进入绘图态可覆盖任意元素绘制；绘图坐标全程浮点精度（前后端取消取整与最小尺寸钳制、线宽按缩放精确补偿），消除缩放倍率导致的“吸附/跳动”观感 | 进行中 |
| 2026-08-26 | v0.59 | 自由画板重构后的交互细化：图片插入按原图宽高比（缺失尺寸退回 3:4）并取消缩放最大/最小限制；修复标记（shape/text）写入旧库报 `NOT NULL constraint failed: canvas_items.asset_id`（新增 v5 迁移重建 canvas_items，真正去除 asset_id 的 NOT NULL 约束）；Frame 内的图片与标记会随 Frame 一起位移（标记创建时按落点记录所属 Frame，Frame 删除时一并移除）；可拖标签改为仅在有搜索词或选中标签筛选时展示；素材拖拽到画板时去掉全屏“松开即可添加图片”上传提示，改为跟随鼠标的轻提示文案；修复手绘笔预览被误判为圆形（pen 工具映射为 freehand 预览） | 进行中 |
| 2026-08-26 | v0.58 | 自由画板重构为 Figma 式无限画布：Frame 降级为可选容器（新建 Page 默认空白无限画布），图片既可在画布上也可拖入 Frame；`canvas_items` 新增 `type`(image/shape/text)/`parent_frame_id`(可空)/`payload`(JSON)，坐标改为自由画布绝对坐标并做旧数据一次性客户端迁移，`asset_id` 改为可空（文本/图形不引用素材）；新增标记图层（每 Page 一个、覆盖所有图层顶上、开关控制显隐与编辑、打开时交互优先标记元素、下层图片锁定），工具含指针/文本/手绘笔/矩形/椭圆/箭头/直线，描边统一颜色+粗细，标记元素可选中/删除/移动/缩放、文本可双击编辑，标记与画板元素同通道服务端多人同步；图片控件简化：去掉名称 caption、去掉旋转、保留置顶/置底/删除，缩放把手改为 hover/选中时才显示；项目素材托盘改 2 列正方形缩略图 + 拖拽插入（去掉名称/点击添加/计数）；托盘新增搜索框（名称+标签模糊）与独立标签下拉筛选；搜索时标签以可拖形式出现，拖标签到画板在落点以网格批量插入同标签全部素材；删除 Frame 仍移除其中图片的布局 | 进行中 |
| 2026-08-25 | v0.57 | AI 打标改为词库优先：第一轮把按使用次数排序的前 200 个标签（剔除素材已用标签）与该素材已有标签一起注入视觉提示词，要求优先原样复用词库标签、只有词库确实没有合适标签时才允许新建（不超过 3 个）、禁止输出词库标签的近义变体、输出前自查去重，词库为空时退回自由生成；未命中标签的模糊候选由“子串互相包含”扩展为“子串包含 + 剥离近义尾缀（感/风/色/调/型等）后相同 + 短词编辑距离相近”，第二轮裁决指令改为优先复用候选；仍保留裁决失败退化为新建、不阻塞整体打标的容错 | 已完成 |
| 2026-08-24 | v0.56 | 密码输入框增加“显示/隐藏”小眼睛：登录页、首次初始化、账号设置（当前/新/确认密码）、成员管理创建成员密码、AI 服务配置 API Key 均可点击切换明文显示，点击眼睛不抢占输入焦点、不触发表单提交 | 已完成 |
| 2026-08-24 | v0.55 | 登录页公告：公告展示在登录页（未登录即可见），仅管理员可编辑（顶部用户菜单 → “登录页公告”，多行纯文本、上限 2000 字，带启用/关闭开关，关闭后登录页不显示、内容保留）；数据存 `app_settings` 键值表，`GET /api/announcement` 公开读取、`PUT /api/announcement` 仅管理员 | 已完成 |
| 2026-08-24 | v0.54 | 新增账号系统：两级角色（管理员/成员）、首次启动引导创建管理员（仅管理员可增删成员）、HttpOnly Cookie Session（记住我 30 天 / 默认 24 小时，SQLite 持久化，重启不丢登录）、全部 `/api/*` 除登录/健康检查外一律要求登录（未登录 401，前端跳登录页）、管理员专属用户管理与 AI 服务配置、登录成功/失败审计（用户名/IP/UA/结果）、素材记录上传者（删除成员后置「已删除用户」快照）、停用立即踢下线、成员可改自己密码与显示名、退出登录；部署脚本改用公开 `/api/health` 做健康检查 | 已完成 |
| 2026-08-24 | v0.53 | 维度预览工具栏新增“图片缩放系数”滑块（50%～200%，100% 为默认 90px）：作为所有预览图片（单张、堆、展开平面卡片）统一的缩放系数，叠加在默认尺寸与堆拖动提示的 132px 放大之上，与场景缩放（间距密度）相互独立；设置写入 localStorage 记住 | 已完成 |
| 2026-08-24 | v0.52 | 修复 3D 预览隔离象限检测失效：场景尺寸 `sceneSize` 原先只在组件挂载时测量一次，切换所选维度使场景平面卸载（mode=0）时 ResizeObserver 会以 0×0 回调将其清零，且平面重建后不再重新测量（或挂载时无平面导致从未测量），此后“即将隔离象限”检测八个象限投影深度全部为 0、永远锁定同一象限、不随视角刷新；改为依赖所选维度数（mode）在平面重建后重新测量并重挂观察器、卸载时忽略 0×0 回调，`computeNearestOctant` 同时增加读取平面当前宽度的兜底 | 已完成 |
| 2026-08-22 | v0.51 | 维度预览：hover 单张或堆且存在前后遮挡时按 `Tab`（`Shift+Tab` 反向）在“当前图 + 屏幕重叠图片（单张与堆）”间按投影深度循环切换焦点，切到单张等同 hover（放大 2.475 倍并淡出自身遮挡者、点击打开详情）、切到堆展开其悬浮平面；循环以鼠标 hover 的图片或堆为锚点、移出即清空；鼠标位于画布内时按 `Tab` 拦截浏览器默认焦点切换（不跳转到侧栏/工具栏）；场景文字（轴端点名称、角落组合、坐标气泡等）在 1D/2D/3D 下禁用鼠标选中 | 已完成 |
| 2026-08-22 | v0.50 | 维度预览素材重叠改为随坐标聚簇堆叠（见[实现记录](./archive/2026-08-22_v0.50_维度预览聚簇堆叠与景深_实现记录.md)）：1D/2D/3D 均按维度坐标空间距离聚簇，合并半径为坐标空间距离、按缩放档位取值且 1D/2D/3D 各自独立（低倍档 1D 各档 40、2D 80/70/50/40、3D 120/110/100/70，125% 起 70÷档位 递减至 25），旋转视角不改变分组（缩放仅跨档重组，坐标变化/维度选择/象限隔离切换亦重组）；素材默认宽度统一 90px（单张/堆/平面卡片一致）；堆收起为“一摞卡片”（最靠前 2～3 张错位叠放 + 数量徽标、位于成员质心）；hover 堆时生成与堆完全重合的悬浮 2D 平面（原堆整体隐藏、无底色），成员在平面内按矩阵展开（列数 = ⌈√成员数⌉，展示全部成员，展开从中心位移、收拢向中心靠拢后再消失，缩放/旋转/平移立即收起）；平面交互与场景完全隔离，平面内卡片 hover 放大 1.86x 不隐藏任何图片，1D/2D 可在平面内拖动卡片改维度值、3D 只点击开详情，1D/2D 拖动接近某堆时该堆放大高亮提示并入；单张 hover 2.475x 放大与遮挡淡出；hover 遮挡淡出扩展至堆（单张 hover 淡出前方堆、堆 hover 淡出平面区域内的前方堆与图片）；以原点为焦点的景深模糊（worldZ<0 的图片/堆按距离渐模糊、最多 5px，hover 取消模糊，隔离象限禁用，带开关）；1D/2D 坐标轴置于图片之后；开发构建增加聚簇范围调试开关（50% 透明红，1D/2D 为圆、3D 为球体，生产构建剔除） | 已完成 |
| 2026-08-22 | v0.49 | 标签筛选与管理模块统一：全部素材页与项目素材页共用同一标签筛选模块（默认展示 2 行、超出可展开收起，已选中标签排最前）；全部素材页视图切换移出标签卡片独立一行靠右展示，标签管理入口移至顶部搜索栏左侧；项目素材库工具栏新增标签管理入口（只展示该项目使用的标签，仅支持重命名/删除，无合并、清理未使用与 AI 服务配置）；标签合并交互改为列表多选 + 底部已选标签组件指定“最终保留的标签”（实时汇总保留/删除两行结果，再点其他标签即替换），合并模式整行可点击增删备选、触发合并的标签一次点击即入备选，未选择保留标签时显示提示文案，面板固定最大高度且列表撑满剩余空间；合并模式右侧新增“包含所选标签的素材”预览画框（占 2/3 宽度，图片两列排列纵向滚动，卡片显示素材名称与所选中的标签） | 已完成 |
| 2026-08-22 | v0.48 | AI 自动打标签与正式标签字典：新增 `tags`/`asset_tags` 标签表（含来源记录与顺序）并迁移删除 `assets.tags` 字符串列；素材详情“全局标签”区新增“AI 打标”按钮，经 OpenAI 兼容接口单图打标（压缩长边≤1024、只输出中文、单次最多 30 个），后端归一精确匹配复用 + 模糊候选由 AI 轻量裁决（裁决失败退化为新建），与人工标签合并去重、单素材总上限 50；标签管理面板新增“AI 服务配置”入口（接口地址/key/模型名，key 服务端保存不回显只显示尾号、测试连接、查看可用模型，环境变量优先，配置文件默认与数据库同目录保证服务用户可写）；全局素材库新增标签管理面板（搜索 + 按使用次数排序，支持重命名、合并、删除、清理未使用） | 已完成 |
| 2026-08-21 | v0.47 | 修复自由画板平移手势被内部元素拦截：`空格 + 左键拖动`和鼠标中键现在可从 Frame、标题、图片及缩放控制点上直接平移 Page 视图，且不会误触元素选择、移动或缩放（原 v0.46，与维度预览 v0.46 并行重号后顺延） | 已完成 |
| 2026-08-20 | v0.46 | 维度预览图片与说明优化：素材图片默认大小改为原来的 50%（41px），hover 放大 4.95 倍，图片按最短边适配完整显示不裁切、无阴影、背景透明；hover/拖动图片时图片正上方显示固定大小的坐标气泡（14px），按素材所在侧显示端点名称与数值绝对值（如“抽象 30”“具象 30”），坐标轴两端只显示名称不显示数值；3D 预览隐藏俯仰/旋转滑条，象限隔离控件移至画面右上角并改为上下排列（描述无边框），hover 检测区域跟随图片放大后的实际大小，3D 模式 hover 图片不再切换为抓取光标（原 v0.44，与画板 v0.44 并行重号后顺延） | 已完成 |
| 2026-08-21 | v0.45 | [素材安全替换与 Page / Frame 自由画板阶段实现记录](./archive/2026-08-21_v0.45_素材安全替换与PageFrame自由画板实现记录.md)：自由画板新增拖动框选、Shift 增减多选、图片组合移动与批量安全移除；图片可对齐图片及 Frame，Frame 移动/缩放可对齐其他 Frame，边缘和中心接近时自动吸附并短暂显示辅助线，Alt/Option 可临时关闭 | 已完成 |
| 2026-08-21 | v0.44 | 自由画板改为 Page → Frame → 素材实例结构：支持多 Page、多 Frame、Frame 重命名/移动/自由缩放/右键安全删除、标题最小可读尺寸；同一素材可在 Page 内无限重复放置，左侧显示实例计数，图片右键只删除当前摆放；旧 `canvas_items` 在事务中无损移除唯一约束并保留全部既有布局 | 已完成 |
| 2026-08-20 | v0.43 | 已有素材支持通过文件选择或剪贴板粘贴安全替换图片，确认前可用 Ctrl/Command+Z 撤销候选；替换保持素材 ID、Metadata、项目引用、维度值和画板元素不变，并以新文件先落盘、数据库切换后清理旧文件的顺序保证失败不损伤原素材；来源链接改为默认只读直接跳转，通过“增加/编辑”显式进入修改状态 | 已完成 |
| 2026-08-18 | v0.42 | 修复高危路径穿越与拒绝服务漏洞：静态资源访问强制限定在 `app/dist/`，媒体存储键仅允许安全单文件名；文件权限错误改为可捕获的异步打开错误，响应流增加 error handler，避免恶意请求导致 Node 进程崩溃；新增 `/etc/passwd` 穿越防护回归测试 | 已完成 |
| 2026-08-18 | v0.41 | 加固首次部署与数据迁移：旧服务停止后使用 SQLite Online Backup API 一致性备份，数据库与媒体改为安全复制并保留旧文件；setup 自动完成首次 ci/typecheck/test/build，显式重启并校验非 root 服务；增加服务名注入防护、仓库外媒体约束、数据库与媒体文件完整性检查及回归测试 | 已完成 |
| 2026-08-18 | v0.40 | 部署流程标准化：`scripts/setup-server.sh`（首次初始化：非 root 服务用户、数据固定 `/var/lib/artdatabase/`、旧库探测/备份/迁移、多库共存停止、`INIT_EMPTY_DB` 显式空库、生成 `/etc/artdatabase/env` 与 systemd unit，支持 `--dry-run`）+ `scripts/deploy.sh`（日常一键部署：读取 env 文件，预检→git ff→ci+typecheck+test+build→重启→后检，任何异常非零退出）+ `scripts/check-production.sh`（只读检查）；生产环境不再自动写入示例数据（仅 `--dev` 或 `SEED_DEMO=1`），根治“部署后变回示例素材库”；新增 `scripts/test-deploy.sh` 部署脚本测试 | 已完成 |
| 2026-08-18 | v0.39 | 修复专注模式（全屏预览）下点击图片无法查看详情：全屏预览层 z-index 由 160 降至 50，避免盖住素材详情抽屉（60）、弹窗（80）与 toast（100）等浮动 UI | 已完成 |
| 2026-08-17 | v0.38 | 修复 3D 预览 Z 轴坐标映射：图片 Z 位置改用动态场景尺寸（sceneSize/1000）替代固定深度系数，图片拉满时与 Z 轴端点严格对齐，隔离象限中心三轴对称 | 已完成 |
| 2026-08-17 | v0.37 | 素材详情大图渐进加载：打开详情先显示缩略图占位，原图后台预加载完成后平滑替换，消除大图下载期间的空白等待 | 已完成 |
| 2026-08-17 | v0.36 | 回收站支持多选、全选与批量操作：卡片复选框 + 工具栏全选（按当前筛选结果）、“已选 N 项”计数、批量恢复与批量永久删除（一次确认，逐条执行并汇总成功/失败数量） | 已完成 |
| 2026-08-17 | v0.35 | 素材详情移除永久删除入口：删除素材统一先软删除进入回收站，恢复或永久删除只在回收站内进行 | 已完成 |
| 2026-08-17 | v0.34 | 移除 `UploadModal` 冗余的客户端缩略图生成逻辑：上传不再携带 `thumbnail` 字段，缩略图统一由服务端 `sharp` 生成（最长边 ≤900px WebP） | 已完成 |
| 2026-08-17 | v0.33 | 新增回收站页面：入口位于左侧导航最底部；列出全部软删除素材，支持恢复（回到原项目）与永久删除（同时清理项目引用与图片文件）；后端新增 `GET /api/trash` | 已完成 |
| 2026-08-17 | v0.32 | 维度预览坐标轴改为有厚度的 1px 细线并去掉刻度数字；坐标角落展示维度端点名称组合（2D 四角两词、3D 的 XY/XZ/YZ 平面四角两词与八个顶点三词）；隔离象限时隐藏其他象限的坐标轴与文字，未隔离时按钮旁显示即将进入的象限名称；Z 轴长度与 XY 轴一致；坐标轴文字放大 | 已完成 |
| 2026-08-17 | v0.31 | 标签筛选由单选改为多选（OR，全局与项目两处），项目素材库新增标签筛选模块；卡片标签不再限制 3 个；标签删除改为原地撤销并在关闭详情时统一结算；标签输入支持方向键选中建议项，输入与待删标签一致时撤销删除；项目视图默认打开维度预览且页签顺序调整为维度预览→自由画板→素材库 | 已完成 |
| 2026-08-17 | v0.30 | 取消 3D 预览中拖动图片修改维度坐标，图片上左键拖动改为旋转视角，单击仍打开素材详情；隔离模式在按钮旁显示当前象限的三个维度端点名称 | 已完成 |
| 2026-08-17 | v0.29 | 3D 维度预览增加最近象限隔离模式，隐藏其他象限图片并将旋转中心切换到已隔离象限中心 | 已完成 |
| 2026-08-17 | v0.28 | 自由画板改为与维度预览一致的剩余浏览器空间自适应布局 | 已完成 |
| 2026-08-17 | v0.27 | 移除维度预览坐标轴两端的箭头，只保留轴线与端点名称 | 已完成 |
| 2026-08-17 | v0.26 | 维度预览增加空格 + 左键拖动平移视图，支持 1D、2D 和 3D，重置视角同时归零平移 | 已完成 |
| 2026-08-17 | v0.25 | 修复详情输入框空格与中文输入法冲突；标签改为 Badge 与模糊匹配；每条维度使用独立图标按钮编辑名称且外层 Slider 保持可调；移除手动保存素材按钮 | 已完成 |
| 2026-08-17 | v0.24 | 素材详情可编辑项目维度两端名称；维度界面值改为 `-100～100`；Slider 改为从中点向当前值填充 | 已完成 |
| 2026-08-17 | v0.23 | 预览页填满浏览器剩余空间；超尺寸坐标系原点固定居中 | 已完成 |
| 2026-08-17 | v0.22 | 统一 1D/2D/3D 的 Hover 放大与遮挡淡出规则，补齐 2D 同深度遮挡 | 已完成 |
| 2026-08-17 | v0.21 | 修复坐标系放大时遗漏 `preserve-3d` 导致第三维被压扁 | 已完成 |
| 2026-08-17 | v0.20 | 维度坐标系放大 2 倍；遮挡图片改为同步淡出；1D Hover 支持隐藏遮挡 | 已完成 |
| 2026-08-17 | v0.19 | [统一维度预览屏幕命中；增加 Hover 放大和深度遮挡](./archive/2026-08-17_v0.19_维度预览Hover与深度遮挡交互修订.md) | 已完成 |
| 2026-08-17 | v0.18 | [详情关闭前自动保存 Metadata](./archive/2026-08-17_v0.18_素材详情关闭自动保存实现记录.md) | 已完成 |
| 2026-08-17 | v0.17 | [重做素材详情、全局标签筛选和延迟删除交互](./archive/2026-08-17_v0.17_素材详情与全局素材交互修订.md) | 已完成 |
| 2026-08-16 | v0.16 | [后端落地为原生 Node 单服务](./archive/2026-08-16_v0.16_后端重构为原生Node单服务实现记录.md) | 已完成 |
| 2026-08-16 | v0.15 | [决定移除 ResourceSpace，改用本地图片存储](./archive/2026-08-16_v0.15_后端图片存储去ResourceSpace重构.md) | 已由 v0.16 实现 |
| 2026-08-14 | v0.14 | [XY 网格不再参与指针命中](./archive/2026-08-14_v0.14_平面指针穿透修订.md) | 已完成 |
| 2026-08-14 | v0.13 | [俯仰范围改为 0°～180°](./archive/2026-08-14_v0.13_俯仰角区间修订.md) | 已完成 |
| 2026-08-14 | v0.12 | [修正水平视角拖动方向](./archive/2026-08-14_v0.12_水平视角拖动方向修订.md) | 已完成 |
| 2026-08-14 | v0.11 | [XY 网格固定为正方形](./archive/2026-08-14_v0.11_XY网格正方形修订.md) | 已完成 |
| 2026-08-14 | v0.10 | [增加 XY 网格、反投影拖动并扩大俯仰范围](./archive/2026-08-14_v0.10_XY网格拖动与俯仰范围修订.md) | 已被后续部分修订 |
| 2026-08-14 | v0.9 | [以页面专注模式取代浏览器全屏](./archive/2026-08-14_v0.9_视图专注模式修订.md) | 已完成 |
| 2026-08-14 | v0.8 | [调整视图布局、缩放和菜单显示](./archive/2026-08-14_v0.8_视图布局与导航修订.md) | 已被 v0.9 部分修订 |
| 2026-08-14 | v0.7 | [扩大三维视角范围](./archive/2026-08-14_v0.7_三维视角范围修订.md) | 已被后续部分修订 |
| 2026-08-14 | v0.6 | [确定三维轴、摄像机与图片拖动规则](./archive/2026-08-14_v0.6_三维预览交互修订.md) | 已被后续部分修订 |
| 2026-08-14 | v0.5 | [实现上传、维度预览与自由画板](./archive/2026-08-14_v0.5_上传预览与画板实现记录.md) | 已完成 |
| 2026-08-14 | v0.4 | [明确项目维度不限、单次预览最多三个](./archive/2026-08-14_v0.4_维度规则澄清.md) | 当前有效 |
| 2026-08-14 | v0.3 | [实现首批项目、维度、网格与搜索功能](./archive/2026-08-14_v0.3_第一批功能实现记录.md) | 已完成 |
| 2026-08-14 | v0.2 | [制定技术验证计划](./archive/2026-08-14_v0.2_技术验证计划.md) | 部分完成后停止 |
| 2026-08-14 | v0.1 | [建立 MVP 需求基线](./archive/2026-08-14_v0.1_需求说明.md) | 已被后续修订 |
