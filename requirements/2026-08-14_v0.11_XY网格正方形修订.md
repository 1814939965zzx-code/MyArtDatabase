# artDatabase XY 网格正方形修订

> 文档版本：v0.11  
> 文档日期：2026-08-14  
> 状态：已完成

## 修订内容

- XY 网格的显示区域固定为正方形，X、Y 两个方向使用相同的视觉尺度。
- 正方形边长同时受预览视口宽度和高度约束，宽屏、窄屏和专注模式下都不会被拉伸成长方形。
- 网格仍保持透明，并继续按 `0～10`、每 `1` 个单位一格显示。

## 验证要求

- 网格渲染区域的宽度与高度必须始终使用同一个尺寸值。
- 调整浏览器尺寸后，网格只允许等比缩放，不允许横向或纵向变形。
- 构建和自动测试通过。
