# artDatabase 视图布局与导航修订

> 文档版本：v0.8  
> 文档日期：2026-08-14  
> 状态：已完成

## 修订内容

- 维度预览保持至少 `640px` 高度，并按浏览器可用高度向上下扩展。
- 维度预览支持进入和退出浏览器全屏模式。
- 鼠标位于坐标视图内时，滚轮用于连续拉近或拉远，缩放范围为 `35%～280%`。
- 左侧项目菜单支持隐藏，隐藏后主工作区自动占用释放的宽度。
- 菜单隐藏后，顶部显示恢复按钮；移动端仍使用抽屉式展开和收起。
- 上述交互只属于当前浏览器的视图状态，不修改项目业务数据。
